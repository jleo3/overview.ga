rails new brablog --skip-bundle --database=postgresql
cp database.yml brablog/config
cd brablog

rails s // show landing page
rails g controller Blog index

subl . &
"hi, potential GA students" > app/views/blog/index.html.erb
// show localhost:3000/blog/index

rails g scaffold Post title:string body:text
rake db:migrate
// show localhost:3000/posts
// create post -> "Hello Brazil!"

// "It just uses ruby EVERYWHERE."


// show github
git init
git add .
git ci -m "first commit"
// create a repo
git remote add origin ...
git push -u origin master
// refresh repo page
// navigate to code
// anyone can see this, fork it, make suggestions, file issues

heroku create
git push heroku master
// show heroku - getting started
// web hosting is _very_ complex. 
// Heroku takes this Herculean task and makes it as simple as 2 commands
// It's an incredible piece of technology, done in the name of Rails
// while that's happening, continue with slides

